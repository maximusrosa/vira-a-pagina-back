import { IsOptional, IsArray, IsNumber } from 'class-validator';

export class CreateModeratorDto {
  // O modelo Moderator não tem atributos próprios além das relações
  // Este DTO pode ser deixado vazio ou com campos opcionais para futuras extensões
}